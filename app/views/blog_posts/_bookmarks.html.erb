<% if user_signed_in? && current_user.bookmarks?(blog_post) %>
  <%= button_to "Remove Bookmark", blog_post_bookmarks_path(blog_post), method: :delete, remote: true %>
<% else %>
  <%= button_to "Bookmark", blog_post_bookmarks_path(blog_post), method: :post, remote: true %>
<% end %>